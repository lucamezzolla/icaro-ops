# Buy aircraft combined filters

Fixes combined filtering in the Buy new aircraft dialog.

The code input now matches ICAO, IATA and model code together, so combinations like Engine = SUPERSONIC and code = SSC keep Concorde visible.

Passenger filters use the best available passenger capacity between standard and max values.
