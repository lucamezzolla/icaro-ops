# Live aircraft compact profit dialog fix

This patch replaces the previous rigid patch script.

It makes the Live aircraft panel show only the compact profit/loss value and opens a Flight economics dialog when that value is clicked.

It is intentionally more tolerant of small differences in `src/js/map-aircraft-left-panel.js`.
